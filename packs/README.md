# Packs

This directory is reserved for Foundry module compendium data.

The first planned module-owned pack is:

- `spell-cores-augments`

Once you export that compendium from Foundry into this repo, add the corresponding `packs` entry to `module.json`.
